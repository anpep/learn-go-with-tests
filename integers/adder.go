// Package integers implements utility routines for manipulating integer values.
package integers

// Add takes two integers returns the result of adding them together.
func Add(a, b int) int {
	return a + b
}
